---
schema_version: "1.0"
task_id: "gemma4-kv-share-no-double-advance"
artifact_hash: "acb4257353b3c3a4efd8007ab5fcd195a06089cc"
artifact_path: "/Users/marvl/Documents/stanford/tiktok-ad-rl/audio_video_rl/forks/easydel:git-show-acb42573"
reviewer_role: "T"
reviewer_agent_blinded: "Reviewer-T"
verdict: "block"
confidence: 84
sealed_at: "2026-04-27T01:03:45-07:00"
sealed_before_other_verdict: true
sealed_before_leon_lean: true
---

## Confidence justification

High on the blocking verifier-file lint failure and on the TransformerCache path; not 100 because I could not inspect `ejkernel.MaskInfo.apply_kv_lengths` internals in this shell.

## Flaws (concrete file:line; required for block verdict)

- `tests/modules/test_gemma4_kv_share_no_double_advance.py:26` — New verifier file imports `pytest` but never uses it; local pre-commit Ruff will reject the changed test file. (severity: block)
  Evidence: `ruff check tests/modules/test_gemma4_kv_share_no_double_advance.py --config pyproject.toml` returned `F401 [*] pytest imported but unused --> tests/modules/test_gemma4_kv_share_no_double_advance.py:26:8`. Ruff selects `F` rules in `pyproject.toml:105-116`; only `__init__.py` ignores `F401` in `pyproject.toml:118-123`; pre-commit runs Ruff on changed Python files in `.pre-commit-config.yaml:23-32`.

## Cannot verify

- Claim: Exact internal semantics of `MaskInfo.apply_kv_lengths`.
  Reason: `ejkernel` was not importable in this shell.
  Would need: Installed `ejkernel==0.0.78` source or a working project environment.

## Verifier files touched by the diff

- `tests/modules/test_gemma4_kv_share_no_double_advance.py`

## Sources consulted

- Tier 1: `git show acb42573` — supports: three changed files and the actual diff shape.
- Tier 1: `easydel/caching/transformer/cache.py:540-584,596-733` — supports: `TransformerCacheView` allocates pre-sized K/V storage, writes via `dynamic_update_slice`, advances `indexs`, applies `mask_info.apply_kv_lengths(..., sliding_window=...)`, and returns runtime-cast K/V.
- Tier 1: `easydel/layers/attention/_flexible.py:1091-1258` — supports: `write_cache=True` default, ragged gate, read-only branch using `_maybe_materialize_kv(...).astype(query.dtype)`, explicit `mask_info = mask_info.apply_kv_lengths(...)`, and later sliding-window reapplication.
- Tier 1: `easydel/modules/gemma4/modeling_gemma4.py:1497-1522,2590-2621` and `easydel/modules/gemma4/gemma4_configuration.py:308-333` — supports: shared layers obtain donor cache views and pass `write_cache=False` only in the shared cached path.
- Tier 1: `easydel/infra/mixins/generation.py:2463-2528,2664-2668,3060-3067` — supports: shared cache-view allocation is skipped and a local generation call also uses the same non-sliding `apply_kv_lengths` argument shape for decode mask preparation.
- Tier 1: `easydel/operations/kernels/ragged_page_attention.py:229-285,555-748,891-918`, `easydel/operations/kernels/unified_attention.py:150-218,359-400`, and `easydel/operations/kernels/paged_flash_attention.py:117-124,180-240` — supports: ragged/unified paged kernels consume cache views/page buffers, so the early return tuple is not the sole source of paged history.
- Tier 1: `easydel/modules/whisper/modeling_whisper.py:291-297`, `easydel/modules/qwen3_next/modeling_qwen3_next.py:1503-1511`, `easydel/modules/openelm/modeling_openelm.py:325-332` — supports: spot-checked other callers omit `write_cache`, so they keep the default write path.
- Tier 1: `tests/modules/test_gemma4_kv_share_no_double_advance.py:57-141` — supports: tests assert prefill donor index equals `q_len`, one decode step advances by one, and no-share layers each advance normally.

## Failed searches (mandatory per Round 6)

- Query: `python inspect.getsourcefile(ejkernel.types.MaskInfo)`
  Tool: local Python
  Outcome: `ModuleNotFoundError: No module named 'ejkernel'`.
- Query: `rg pytest|addopts|filterwarnings pyproject.toml pytest.ini tox.ini ...`
  Tool: `rg`
  Outcome: reported missing optional `pytest.ini`/`tox.ini`; reran against existing files and found pytest config in `pyproject.toml`.

## Notes (no rationalization, no Gish-gallop)

A. For the standard TransformerCache path, the read-only branch is structurally correct: donor writes populate `cache_view.key/value` and advance `indexs`; the shared layer then materializes those buffers, casts to `query.dtype`, and applies lengths with the donor's post-write `indexs`. Omitting `sliding_window` in that call is consistent with later `_apply_sliding_window` execution when Gemma4 passes `sliding_window_for_kernel`.

B. Ragged/unified cache handling does not leave the paged-cache history solely in stale returned K/V: v2/unified/paged-flash read page buffers from `cache_view`; v3 also receives current `key/value` and page buffers. I did not find a concrete ragged regression in this diff.

C. Decode step 0 is correct for shared layers because `donor_cache_views` is filled after the donor layer and read before the shared layer; `cache_view.indexs` is already post-write when `apply_kv_lengths` runs.

D. Whisper, Qwen3-Next, and OpenELM keep default `write_cache=True`; no compatibility break found.

E. If only the Gemma4 `write_cache=False` argument is reverted, the shared path calls the default write path, which reaches `_handle_cache_concat` and `cache_view.concatenate_to_cache`; the prefill assertion at test line 78 would see `[8]`, not `[4]`.

F. Round 5 GO tweaks are present: ragged branch is gated by `write_cache`, default is `True`, and the read-only branch preserves explicit `mask_info = mask_info.apply_kv_lengths(...)`.
